import LoginCard from "@/components/auth/login-card";

export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[radial-gradient(circle_at_center,_rgba(35,53,82,0.32),_transparent_35%),_var(--bg)] px-4">
      <LoginCard />
    </main>
  );
}